// Database configuration
DB_HOST=hostname (e.g., localhost)
DB_USER=username (e.g., root)
DB_PASSWORD=password (e.g., empty for no password)
DB_NAME=database_name (e.g., case_generator)
DB_PORT=3306 (default MySQL port)

// API configuration
PORT=3000 (default port for the API)